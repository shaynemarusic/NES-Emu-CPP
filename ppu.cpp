#include "ppu.h"
#include "./SDL2/include/SDL.h"

// This specifically is the 2C02G palette with emphasized variants from the nes wiki
constexpr uint8_t sys_palette[512][3] = {
{0x62, 0x62, 0x62}, {0x00, 0x1C, 0x95}, {0x19, 0x04, 0xAC}, {0x42, 0x00, 0x9D}, {0x61, 0x00, 0x6B}, 
{0x6E, 0x00, 0x25}, {0x65, 0x05, 0x00}, {0x49, 0x1E, 0x00}, {0x22, 0x37, 0x00}, {0x00, 0x49, 0x00}, 
{0x00, 0x4F, 0x00}, {0x00, 0x48, 0x16}, {0x00, 0x35, 0x5E}, {0x00, 0x00, 0x00}, {0x00, 0x00, 0x00}, 
{0x00, 0x00, 0x00}, {0xAB, 0xAB, 0xAB}, {0x0C, 0x4E, 0xDB}, {0x3D, 0x2E, 0xFF}, {0x71, 0x15, 0xF3}, 
{0x9B, 0x0B, 0xB9}, {0xB0, 0x12, 0x62}, {0xA9, 0x27, 0x04}, {0x89, 0x46, 0x00}, {0x57, 0x66, 0x00}, 
{0x23, 0x7F, 0x00}, {0x00, 0x89, 0x00}, {0x00, 0x83, 0x32}, {0x00, 0x6D, 0x90}, {0x00, 0x00, 0x00}, 
{0x00, 0x00, 0x00}, {0x00, 0x00, 0x00}, {0xFF, 0xFF, 0xFF}, {0x57, 0xA5, 0xFF}, {0x82, 0x87, 0xFF}, 
{0xB4, 0x6D, 0xFF}, {0xDF, 0x60, 0xFF}, {0xF8, 0x63, 0xC6}, {0xF8, 0x74, 0x6D}, {0xDE, 0x90, 0x20}, 
{0xB3, 0xAE, 0x00}, {0x81, 0xC8, 0x00}, {0x56, 0xD5, 0x22}, {0x3D, 0xD3, 0x6F}, {0x3E, 0xC1, 0xC8}, 
{0x4E, 0x4E, 0x4E}, {0x00, 0x00, 0x00}, {0x00, 0x00, 0x00}, {0xFF, 0xFF, 0xFF}, {0xBE, 0xE0, 0xFF}, 
{0xCD, 0xD4, 0xFF}, {0xE0, 0xCA, 0xFF}, {0xF1, 0xC4, 0xFF}, {0xFC, 0xC4, 0xEF}, {0xFD, 0xCA, 0xCE}, 
{0xF5, 0xD4, 0xAF}, {0xE6, 0xDF, 0x9C}, {0xD3, 0xE9, 0x9A}, {0xC2, 0xEF, 0xA8}, {0xB7, 0xEF, 0xC4}, 
{0xB6, 0xEA, 0xE5}, {0xB8, 0xB8, 0xB8}, {0x00, 0x00, 0x00}, {0x00, 0x00, 0x00}, {0x64, 0x47, 0x40}, 
{0x00, 0x09, 0x70}, {0x1A, 0x00, 0x85}, {0x41, 0x00, 0x78}, {0x5E, 0x00, 0x4D}, {0x6C, 0x00, 0x0F}, 
{0x64, 0x00, 0x00}, {0x49, 0x13, 0x00}, {0x23, 0x28, 0x00}, {0x00, 0x36, 0x00}, {0x00, 0x39, 0x00}, 
{0x00, 0x30, 0x00}, {0x00, 0x1E, 0x3F}, {0x00, 0x00, 0x00}, {0x00, 0x00, 0x00}, {0x00, 0x00, 0x00}, 
{0xAC, 0x83, 0x81}, {0x11, 0x31, 0xAE}, {0x40, 0x16, 0xCF}, {0x72, 0x03, 0xC4}, {0x9A, 0x00, 0x91}, 
{0xAE, 0x04, 0x42}, {0xA7, 0x19, 0x00}, {0x88, 0x35, 0x00}, {0x58, 0x51, 0x00}, {0x25, 0x65, 0x00}, 
{0x00, 0x6B, 0x00}, {0x00, 0x62, 0x18}, {0x00, 0x4D, 0x6B}, {0x01, 0x00, 0x00}, {0x00, 0x00, 0x00}, 
{0x00, 0x00, 0x00}, {0xFE, 0xCE, 0xD2}, {0x5D, 0x7E, 0xE8}, {0x87, 0x63, 0xFF}, {0xB7, 0x4D, 0xFF}, 
{0xE0, 0x43, 0xE3}, {0xF7, 0x47, 0x9B}, {0xF4, 0x58, 0x47}, {0xDC, 0x73, 0x01}, {0xB1, 0x8E, 0x00}, 
{0x81, 0xA4, 0x00}, {0x58, 0xAE, 0x07}, {0x42, 0xAA, 0x4F}, {0x44, 0x99, 0xA2}, {0x50, 0x35, 0x2D}, 
{0x00, 0x00, 0x00}, {0x00, 0x00, 0x00}, {0xFE, 0xCE, 0xD2}, {0xC0, 0xB1, 0xD7}, {0xCF, 0xA7, 0xE9}, 
{0xE1, 0x9D, 0xEA}, {0xF1, 0x99, 0xDC}, {0xFB, 0x99, 0xC1}, {0xFB, 0x9E, 0xA0}, {0xF3, 0xA9, 0x85}, 
{0xE4, 0xB3, 0x73}, {0xD2, 0xBD, 0x72}, {0xC2, 0xC1, 0x80}, {0xB8, 0xC1, 0x9B}, {0xB8, 0xBC, 0xBB}, 
{0xB9, 0x8F, 0x8E}, {0x00, 0x00, 0x00}, {0x00, 0x00, 0x00}, {0x3D, 0x5D, 0x36}, {0x00, 0x19, 0x73}, 
{0x02, 0x01, 0x84}, {0x25, 0x00, 0x72}, {0x3F, 0x00, 0x43}, {0x4A, 0x00, 0x05}, {0x44, 0x04, 0x00}, 
{0x2C, 0x1D, 0x00}, {0x0A, 0x35, 0x00}, {0x00, 0x46, 0x00}, {0x00, 0x4D, 0x00}, {0x00, 0x45, 0x03}, 
{0x00, 0x32, 0x44}, {0x00, 0x00, 0x00}, {0x00, 0x00, 0x00}, {0x00, 0x00, 0x00}, {0x7A, 0xA1, 0x6A}, 
{0x00, 0x47, 0xAD}, {0x1F, 0x28, 0xC9}, {0x4D, 0x11, 0xB9}, {0x71, 0x08, 0x82}, {0x83, 0x10, 0x33}, 
{0x7D, 0x26, 0x00}, {0x61, 0x44, 0x00}, {0x37, 0x63, 0x00}, {0x08, 0x7B, 0x00}, {0x00, 0x85, 0x00}, 
{0x00, 0x7D, 0x17}, {0x00, 0x67, 0x6C}, {0x00, 0x00, 0x00}, {0x00, 0x00, 0x00}, {0x00, 0x00, 0x00}, 
{0xC7, 0xF0, 0xAF}, {0x32, 0x9B, 0xD9}, {0x5A, 0x7D, 0xFE}, {0x88, 0x65, 0xF9}, {0xAF, 0x5A, 0xCD}, 
{0xC5, 0x5D, 0x84}, {0xC4, 0x6F, 0x32}, {0xAC, 0x8B, 0x00}, {0x85, 0xA9, 0x00}, {0x57, 0xC1, 0x00}, 
{0x2F, 0xCC, 0x00}, {0x1A, 0xC9, 0x41}, {0x1B, 0xB7, 0x94}, {0x2B, 0x49, 0x26}, {0x00, 0x00, 0x00}, 
{0x00, 0x00, 0x00}, {0xC7, 0xF0, 0xAF}, {0x8B, 0xD2, 0xBA}, {0x9A, 0xC7, 0xCB}, {0xAC, 0xBE, 0xCC}, 
{0xBC, 0xB9, 0xBE}, {0xC5, 0xB9, 0xA3}, {0xC6, 0xBF, 0x83}, {0xBE, 0xC9, 0x67}, {0xAF, 0xD4, 0x56}, 
{0x9E, 0xDD, 0x55}, {0x8D, 0xE2, 0x63}, {0x84, 0xE2, 0x7F}, {0x83, 0xDC, 0x9E}, {0x86, 0xAE, 0x75}, 
{0x00, 0x00, 0x00}, {0x00, 0x00, 0x00}, {0x44, 0x46, 0x2F}, {0x00, 0x08, 0x66}, {0x08, 0x00, 0x77}, 
{0x29, 0x00, 0x68}, {0x42, 0x00, 0x3D}, {0x4D, 0x00, 0x00}, {0x46, 0x00, 0x00}, {0x2F, 0x12, 0x00}, 
{0x0E, 0x26, 0x00}, {0x00, 0x34, 0x00}, {0x00, 0x38, 0x00}, {0x00, 0x2F, 0x00}, {0x00, 0x1D, 0x3A}, 
{0x00, 0x00, 0x00}, {0x00, 0x00, 0x00}, {0x00, 0x00, 0x00}, {0x81, 0x81, 0x65}, {0x00, 0x30, 0xA0}, 
{0x27, 0x16, 0xBB}, {0x53, 0x02, 0xAC}, {0x76, 0x00, 0x79}, {0x87, 0x04, 0x2C}, {0x80, 0x18, 0x00}, 
{0x64, 0x34, 0x00}, {0x3A, 0x4F, 0x00}, {0x0E, 0x63, 0x00}, {0x00, 0x69, 0x00}, {0x00, 0x61, 0x12}, 
{0x00, 0x4C, 0x62}, {0x00, 0x00, 0x00}, {0x00, 0x00, 0x00}, {0x00, 0x00, 0x00}, {0xCC, 0xCA, 0xAC}, 
{0x3C, 0x7C, 0xD0}, {0x64, 0x61, 0xF3}, {0x90, 0x4B, 0xEE}, {0xB6, 0x42, 0xC3}, {0xCA, 0x46, 0x7C}, 
{0xC7, 0x57, 0x2E}, {0xB0, 0x71, 0x00}, {0x89, 0x8D, 0x00}, {0x5C, 0xA2, 0x00}, {0x36, 0xAB, 0x00}, 
{0x22, 0xA7, 0x3F}, {0x25, 0x96, 0x8E}, {0x32, 0x34, 0x1F}, {0x00, 0x00, 0x00}, {0x00, 0x00, 0x00}, 
{0xCC, 0xCA, 0xAC}, {0x93, 0xAE, 0xB6}, {0xA1, 0xA3, 0xC6}, {0xB3, 0x9A, 0xC7}, {0xC3, 0x96, 0xB8}, 
{0xCB, 0x96, 0x9D}, {0xCB, 0x9C, 0x7F}, {0xC3, 0xA6, 0x64}, {0xB4, 0xB1, 0x54}, {0xA3, 0xBA, 0x53}, 
{0x93, 0xBE, 0x63}, {0x8B, 0xBD, 0x7D}, {0x8B, 0xB8, 0x9B}, {0x8D, 0x8D, 0x71}, {0x00, 0x00, 0x00}, 
{0x00, 0x00, 0x00}, {0x4C, 0x49, 0x78}, {0x00, 0x0F, 0x9E}, {0x13, 0x00, 0xB3}, {0x37, 0x00, 0xA3}, 
{0x51, 0x00, 0x72}, {0x5A, 0x00, 0x2F}, {0x4F, 0x00, 0x00}, {0x34, 0x0C, 0x00}, {0x0F, 0x21, 0x00}, 
{0x00, 0x31, 0x00}, {0x00, 0x38, 0x00}, {0x00, 0x33, 0x27}, {0x00, 0x24, 0x6A}, {0x00, 0x00, 0x00}, 
{0x00, 0x00, 0x00}, {0x00, 0x00, 0x00}, {0x88, 0x89, 0xC3}, {0x04, 0x3A, 0xE8}, {0x32, 0x1E, 0xFF}, 
{0x62, 0x07, 0xFB}, {0x86, 0x00, 0xC1}, {0x95, 0x01, 0x6D}, {0x8B, 0x14, 0x15}, {0x6A, 0x2F, 0x00}, 
{0x3D, 0x4B, 0x00}, {0x0E, 0x61, 0x00}, {0x00, 0x6B, 0x00}, {0x00, 0x67, 0x4B}, {0x00, 0x55, 0xA2}, 
{0x00, 0x00, 0x0D}, {0x00, 0x00, 0x00}, {0x00, 0x00, 0x00}, {0xD1, 0xD7, 0xFF}, {0x42, 0x87, 0xFF}, 
{0x6B, 0x6B, 0xFF}, {0x9A, 0x54, 0xFF}, {0xC1, 0x47, 0xFF}, {0xD5, 0x49, 0xD4}, {0xD1, 0x59, 0x7F}, 
{0xB7, 0x72, 0x39}, {0x8F, 0x8F, 0x12}, {0x60, 0xA6, 0x15}, {0x39, 0xB2, 0x42}, {0x25, 0xB0, 0x8D}, 
{0x28, 0xA1, 0xE2}, {0x3A, 0x36, 0x62}, {0x00, 0x00, 0x00}, {0x00, 0x00, 0x00}, {0xD1, 0xD7, 0xFF}, 
{0x98, 0xBA, 0xFF}, {0xA7, 0xAF, 0xFF}, {0xB9, 0xA6, 0xFF}, {0xC9, 0xA0, 0xFF}, {0xD2, 0xA0, 0xFF}, 
{0xD2, 0xA6, 0xE3}, {0xCA, 0xAF, 0xC6}, {0xBB, 0xBB, 0xB6}, {0xA9, 0xC4, 0xB4}, {0x99, 0xC9, 0xC3}, 
{0x90, 0xC9, 0xDE}, {0x8F, 0xC4, 0xFE}, {0x94, 0x95, 0xCF}, {0x00, 0x00, 0x00}, {0x00, 0x00, 0x00}, 
{0x4B, 0x3C, 0x4E}, {0x00, 0x04, 0x77}, {0x12, 0x00, 0x8B}, {0x35, 0x00, 0x7D}, {0x4E, 0x00, 0x53}, 
{0x57, 0x00, 0x18}, {0x4E, 0x00, 0x00}, {0x33, 0x09, 0x00}, {0x0F, 0x1D, 0x00}, {0x00, 0x2B, 0x00}, 
{0x00, 0x2F, 0x00}, {0x00, 0x28, 0x0B}, {0x00, 0x19, 0x47}, {0x00, 0x00, 0x00}, {0x00, 0x00, 0x00}, 
{0x00, 0x00, 0x00}, {0x88, 0x75, 0x8F}, {0x05, 0x2A, 0xB8}, {0x32, 0x10, 0xD6}, {0x60, 0x00, 0xCA}, 
{0x83, 0x00, 0x97}, {0x92, 0x00, 0x4B}, {0x88, 0x0E, 0x00}, {0x69, 0x29, 0x00}, {0x3C, 0x44, 0x00}, 
{0x0F, 0x57, 0x00}, {0x00, 0x5E, 0x00}, {0x00, 0x58, 0x28}, {0x00, 0x45, 0x78}, {0x00, 0x00, 0x00}, 
{0x00, 0x00, 0x00}, {0x00, 0x00, 0x00}, {0xD2, 0xBD, 0xDC}, {0x44, 0x73, 0xF5}, {0x6C, 0x58, 0xFF}, 
{0x9A, 0x42, 0xFF}, {0xBF, 0x38, 0xEA}, {0xD3, 0x3C, 0xA3}, {0xCF, 0x4C, 0x52}, {0xB6, 0x65, 0x10}, 
{0x8E, 0x80, 0x00}, {0x60, 0x95, 0x00}, {0x3B, 0x9F, 0x1B}, {0x27, 0x9C, 0x62}, {0x2B, 0x8C, 0xB3}, 
{0x39, 0x2B, 0x3C}, {0x00, 0x00, 0x00}, {0x00, 0x00, 0x00}, {0xD2, 0xBD, 0xDC}, {0x99, 0xA2, 0xE4}, 
{0xA8, 0x98, 0xF3}, {0xBA, 0x8F, 0xF4}, {0xC9, 0x8A, 0xE5}, {0xD2, 0x8A, 0xCA}, {0xD2, 0x90, 0xAB}, 
{0xCA, 0x9A, 0x91}, {0xBB, 0xA4, 0x81}, {0xA9, 0xAD, 0x80}, {0x9A, 0xB2, 0x8F}, {0x91, 0xB1, 0xAA}, 
{0x91, 0xAC, 0xC9}, {0x94, 0x81, 0x9B}, {0x00, 0x00, 0x00}, {0x00, 0x00, 0x00}, {0x39, 0x46, 0x4A}, 
{0x00, 0x0D, 0x7A}, {0x03, 0x00, 0x8A}, {0x25, 0x00, 0x78}, {0x3E, 0x00, 0x4A}, {0x47, 0x00, 0x0F}, 
{0x3F, 0x00, 0x00}, {0x27, 0x0D, 0x00}, {0x06, 0x21, 0x00}, {0x00, 0x31, 0x00}, {0x00, 0x37, 0x00}, 
{0x00, 0x31, 0x12}, {0x00, 0x22, 0x4E}, {0x00, 0x00, 0x00}, {0x00, 0x00, 0x00}, {0x00, 0x00, 0x00}, 
{0x70, 0x83, 0x86}, {0x00, 0x36, 0xB9}, {0x1F, 0x1A, 0xD3}, {0x4C, 0x04, 0xC3}, {0x6E, 0x00, 0x8C}, 
{0x7D, 0x02, 0x40}, {0x75, 0x15, 0x00}, {0x59, 0x2F, 0x00}, {0x2F, 0x4B, 0x00}, {0x02, 0x60, 0x00}, 
{0x00, 0x69, 0x00}, {0x00, 0x64, 0x2D}, {0x00, 0x51, 0x7C}, {0x00, 0x00, 0x00}, {0x00, 0x00, 0x00}, 
{0x00, 0x00, 0x00}, {0xB7, 0xCE, 0xCD}, {0x2F, 0x80, 0xEF}, {0x57, 0x64, 0xFF}, {0x83, 0x4E, 0xFF}, 
{0xA7, 0x44, 0xDF}, {0xBB, 0x46, 0x97}, {0xB8, 0x57, 0x48}, {0xA0, 0x70, 0x07}, {0x78, 0x8C, 0x00}, 
{0x4C, 0xA2, 0x00}, {0x28, 0xAD, 0x16}, {0x14, 0xAA, 0x5E}, {0x17, 0x9A, 0xAD}, {0x28, 0x34, 0x39}, 
{0x00, 0x00, 0x00}, {0x00, 0x00, 0x00}, {0xB7, 0xCE, 0xCD}, {0x80, 0xB1, 0xD8}, {0x8F, 0xA6, 0xE7}, 
{0xA0, 0x9E, 0xE7}, {0xAF, 0x99, 0xD9}, {0xB8, 0x99, 0xBE}, {0xB9, 0x9F, 0x9F}, {0xB0, 0xA9, 0x84}, 
{0xA1, 0xB4, 0x74}, {0x90, 0xBC, 0x74}, {0x81, 0xC1, 0x82}, {0x78, 0xC1, 0x9D}, {0x78, 0xBB, 0xBD}, 
{0x7C, 0x8F, 0x91}, {0x00, 0x00, 0x00}, {0x00, 0x00, 0x00}, {0x3D, 0x3D, 0x3D}, {0x00, 0x05, 0x6C}, 
{0x06, 0x00, 0x7C}, {0x27, 0x00, 0x6D}, {0x40, 0x00, 0x43}, {0x49, 0x00, 0x08}, {0x40, 0x00, 0x00}, 
{0x29, 0x0A, 0x00}, {0x08, 0x1D, 0x00}, {0x00, 0x2B, 0x00}, {0x00, 0x2F, 0x00}, {0x00, 0x29, 0x06}, 
{0x00, 0x19, 0x40}, {0x00, 0x00, 0x00}, {0x00, 0x00, 0x00}, {0x00, 0x00, 0x00}, {0x76, 0x76, 0x76}, 
{0x00, 0x2B, 0xA8}, {0x24, 0x10, 0xC2}, {0x4F, 0x00, 0xB4}, {0x71, 0x00, 0x81}, {0x80, 0x00, 0x36}, 
{0x78, 0x10, 0x00}, {0x5B, 0x2A, 0x00}, {0x31, 0x45, 0x00}, {0x06, 0x58, 0x00}, {0x00, 0x5F, 0x00}, 
{0x00, 0x58, 0x1F}, {0x00, 0x45, 0x6C}, {0x00, 0x00, 0x00}, {0x00, 0x00, 0x00}, {0x00, 0x00, 0x00}, 
{0xBD, 0xBD, 0xBD}, {0x35, 0x72, 0xDE}, {0x5D, 0x58, 0xFF}, {0x88, 0x43, 0xFA}, {0xAC, 0x39, 0xCF}, 
{0xBF, 0x3D, 0x89}, {0xBC, 0x4D, 0x3B}, {0xA3, 0x66, 0x00}, {0x7C, 0x81, 0x00}, {0x51, 0x96, 0x00}, 
{0x2D, 0xA0, 0x0A}, {0x1A, 0x9C, 0x50}, {0x1D, 0x8B, 0x9E}, {0x2C, 0x2C, 0x2C}, {0x00, 0x00, 0x00}, 
{0x00, 0x00, 0x00}, {0xBD, 0xBD, 0xBD}, {0x87, 0xA2, 0xC7}, {0x95, 0x97, 0xD7}, {0xA6, 0x8F, 0xD7}, 
{0xB5, 0x8A, 0xC8}, {0xBE, 0x8B, 0xAD}, {0xBE, 0x91, 0x8F}, {0xB5, 0x9A, 0x75}, {0xA7, 0xA4, 0x65}, 
{0x95, 0xAD, 0x65}, {0x87, 0xB2, 0x74}, {0x7E, 0xB1, 0x8E}, {0x7E, 0xAB, 0xAD}, {0x81, 0x81, 0x81}, 
{0x00, 0x00, 0x00}, {0x00, 0x00, 0x00}
};

// Constructors

// Default constructor
PPU::PPU() {
    std::fill_n(memory, 65536, 0);
    vertical_mirroring = 0;
    memory_mapper = 0;

    // Initialize frame buffer
    for (int i = 0; i < 256; i++) {
        std::fill_n(frame_buffer[i], 240, 0);
    }

    // Set MMIO registers
    ppuctrl = 0;
    ppumask = 0;
    ppustatus = 0;
    oamaddr = 0;
    oamdata = 0;
    ppuscroll = 0;
    ppuaddr = 0;
    ppudata = 0;
    oamdma = 0;
    
    // Set internal registers and other variables
    v = 0;
    t = 0;
    w = 0;
    x = 0;
    current_nametable_byte = 0;

    // Set state variables
    scanline = 261;
    dot = 0;
    frame = 0;

    // Set bus
    address_bus = 0;
}

// Setters + Getters
void PPU::set_memory_mapper(int mapper) { memory_mapper = mapper; }

int PPU::get_mapper() const { return memory_mapper; }

void PPU::set_mirroring(int vert) { vertical_mirroring = vert; }

int PPU::get_mirroring() const { return vertical_mirroring; }

// Modifies t - sets the nametable select bits (bits 10-11) with the 2 least significant bits
// One special effect is if the vblank nmi flag is flipped from 0 to 1 while ppustatus' vblank flag is set, an NMI will be immediately
// triggered
void PPU::set_ppuctrl(uint16_t value) { 
    
    uint8_t old_nmi = ppuctrl & 0x80;
    uint8_t new_nmi = value & 0x80;
    if (old_nmi == 0 && new_nmi != old_nmi) nmi_trigger = true;
    ppuctrl = value;
    t = (t & 0x73FF) | ((value & 3) << 10);

}

uint8_t PPU::get_ppuctrl() const { return ppuctrl; }

void PPU::set_ppumask(uint8_t value) { ppumask = value; }

uint8_t PPU::get_ppumask() const { return ppumask; }

void PPU::set_ppustatus(uint8_t value) { ppustatus = value; }

// This sets w to 0 and clears the VBlank flag
uint8_t PPU::get_ppustatus() {

    w = false;
    uint8_t current_status = ppustatus;
    ppustatus &= ~0x80;
    return current_status; 

}

void PPU::set_oamaddr(uint8_t value) { oamaddr = value; }

uint8_t PPU::get_oamaddr() const { return oamaddr; }

void PPU::set_oamdata(uint8_t value) { oamdata = value; }

uint8_t PPU::get_oamdata() const { return oamdata; }

// Toggles w
// Modifies t and x
void PPU::set_ppuscroll(uint16_t value) { 

    // First write
    if (!w) {

        ppuscroll = (ppuscroll & 0xFF00) | value;
        // x is set to the lower 3 bits of value
        x = value & 7;
        // The lower 5 bits of t (coarse x bits) are set to the upper 5 bits of value
        t = (t & 0x7EE0) | (value >> 3);

    }
    // Second write
    else {
        
        ppuscroll = (ppuscroll & 0xFF) | (value << 8);
        // Upper 3 bits of t (fine y bits) are set to the lower 3 bits of value, bits 5-9 of t (coarse y bits) are 
        // set to the next 5 bits of value
        t = (t & 0x4C1F) | ((value & 0x7) << 12) | ((value & 0xF8) << 2);

    }
    w = !w;

 }

uint16_t PPU::get_ppuscroll() const { return ppuscroll; }

// Toggles w
// Modifies t. On the second write, t is copied to v - this copying occurs about a dot after the actual write
void PPU::set_ppuaddr(uint16_t value) {

    // First write
    if (!w) {
        
        ppuaddr = (ppuaddr & 0xFF00) | value;
        // bits 8-13 of t are set to the 6 least significant bits of value, the most significant bit is cleared
        t = (t & 0x1FF) | ((value & 0x3F) << 8);
    
    }
    // Second write
    else {
        
        ppuaddr = (ppuaddr & 0xFF) | (value << 8);
        // The lower byte of t is set to value
        t = (t & 0x7F00) | value;
        // Copy to v - not sure how important the timing of this is, but I'll just assume it's important
        // TODO
        v = t;
    
    }
    w = !w;

}

uint16_t PPU::get_ppuaddr() const { return ppuaddr; }

// This causes v to increment depending on the value of ppuctrl and has some other funky effects that I will mess with later
void PPU::set_ppudata(uint8_t value) {

    write(v, value);
    // This is a quirk of messing with this register during rendering
    // If this happens on a dot where an increment to v is supposed to happen anyway, we don't increment twice, but given how niche
    // that is and how much of a pain it would be to deal with, I don't really care to deal with that
    if (is_render_enabled() && (scanline == 261 || scanline < 239)) {
        increment_coarse_x();
        increment_fine_y();
    }
    else {
        uint8_t increment_amount = ((ppuctrl & 4) >> 2) == 1 ? 32 : 1;
        v = (v + increment_amount) & 0x7FFF;
    }
    ppudata = value;

}

// This also causes v to increment
// If we read from palette memory we don't have to deal with the read buffer
uint8_t PPU::get_ppudata() { 
    
    if (is_render_enabled() && (scanline == 261 || scanline < 239)) {
        increment_coarse_x();
        increment_fine_y();
    }
    else {
        uint8_t increment_amount = ((ppuctrl & 4) >> 2) == 1 ? 32 : 1;
        v = (v + increment_amount) & 0x7FFF;
    }
    // Apply greyscaling
    if (v >= 0x3F00 && v <= 0x3FFF && (ppumask & 1) == 1) {
        return read(v) & 0x30;
    }
    uint8_t temp = read_buffer;
    read_buffer = read(v);
    return temp;

}

void PPU::set_oamdma(uint8_t value) { oamdma = value; }

uint8_t PPU::get_oamdma() const { return oamdma; }

// Execution related functions

// Runs the PPU for one dot/PPU cycle - a PPU cycle is a third of a CPU cycle
void PPU::tick() {
    // Need to consider where in the rendering process we are - e.g. what scanline we are on/where in the scanline we are at
    // Defer updating the window until a full pass of the screen has been made
    // We will keep pixel information in a buffer

    // Disarm nmi trigger
    nmi_trigger = false;

    // These are the visible scanlines - the ppu actually modifies visible pixels in this section
    if (scanline <= 239) {
        // Idle cycle - the address bus is loaded with the address to the low background tile byte
        // If the frame is even and rendering is enabled, we skip this cycle
        if (dot == 0 && is_render_enabled() && frame % 2 == 0) fetch_patterntable_low_address();

        // Regular execution - a pixel is selected from the shift registers and it is updated in the pixel buffer
        // At the same time, the PPU is continually fetching data for the next set of 8 pixels
        else if (dot < 257) {
            // "Draw" a pixel
            update_pixel();

            // Shift the shift registers - Note that this occurs regardless of if rendering is enabled or not
            shift_srs();

            int dot_mod = dot % 8;
            // Check if we need to load shift registers
            // Every time a nametable byte is fetched with the exception of cycles 1 and 321, the fetched data is loaded into
            // the appropriate shift register
            if (dot != 1 && dot_mod == 1) load_shift_registers();

            // Fetching
            fetch();

            // Check if we need to increment coarse x - this is done every 8 cycles 
            if (dot_mod == 0) increment_coarse_x();

            // Check if we need to increment fine y - this is done only on cycle 256
            if (dot == 256) increment_fine_y();
        }
        // TODO
        else if (dot < 321) {

        }
        // We fetch data for the next scanline and do nothing else
        else if (dot < 337) fetch();
        // During the last two dots of a scanline the PPU performs two useless nametable fetches. This behavior is used in some games
        else if (dot % 2 == 1 && is_render_enabled()) fetch_nametable_address();
        else if (is_render_enabled()) current_nametable_byte = read(address_bus);
    }
    // The post-render scanline - the ppu just idles during this scanline
    else if (scanline == 240) {
        if (dot == 0 && is_render_enabled()) fetch_patterntable_low_address();
    }
    // This is the beginning of VBlank period - the VBlank is set during the second tick of scanline 241 and an NMI is triggered
    else if (scanline == 241) {
        if (dot == 1) {
            ppustatus |= 0x80;
            nmi_trigger = true;
        }
    }
    // VBlank - the PPU essentially does nothing until it reaches scanline 261
    else if (scanline < 261) {

    }
    // Scanline 261 - This is the prerender scanline (aka scanline -1). The PPU loads data to prepare for rendering in scanline 0
    // Length of this scanline is dependent on if an even or odd frame is being rendered
    else {
        // Behaves the same as the visible scanlines except no pixels are actually updated
        if (dot > 0 && dot < 257) {
            // Clear VBlank flag, Sprite 0, and Sprite overflow flags in ppustatus
            if (dot == 1) ppustatus = 0;

            // Shift the shift registers - Note that this occurs regardless of if rendering is enabled or not
            shift_srs();

            int dot_mod = dot % 8;
            // Check if we need to load shift registers
            // Every time a nametable byte is fetched with the exception of cycles 1 and 321, the fetched data is loaded into
            // the appropriate shift register
            if (dot != 1 && dot_mod == 1) load_shift_registers();

            // Fetching
            fetch();

            // Check if we need to increment coarse x - this is done every 8 cycles 
            if (dot_mod == 0) increment_coarse_x();

            // Check if we need to increment fine y - this is done only on cycle 256
            if (dot == 256) increment_fine_y();
        }
        // TODO
        else if (dot < 321) {
            // Copy over the horizontal component of t to the v
            if (dot == 257 && is_render_enabled()) v = (v & 0x7BE0) | (t & 0x41F);

            // If rendering is enabled we reload the vertical scroll bits of v
            if (dot >= 280 && dot < 305 && is_render_enabled()) v = (v & 0x41F) | (t & 0x7BE0);

        }
        // We fetch data for the next scanline and do nothing else
        else if (dot < 337) {
            fetch();
        }
        // During the last two dots of a scanline the PPU performs two useless nametable fetches. This behavior is used in some games
        else if (dot % 2 == 1 && is_render_enabled()){
            fetch_nametable_address();
        }
        else if (is_render_enabled()) {
            current_nametable_byte = read(address_bus);
        }
    }

    dot++;
    // Reached the end of a scanline
    if (dot == 341) {
        dot = 0;
        scanline++;
    }

    // Reached the end of the final scanline
    if (scanline == 262) {
        scanline = 0;
        frame++;
    }
}

// Tile fetching related functions

// The nametable address is essentially just v ignoring the 3 most significant bits (Y fine) and or'ed with 0x2000
void PPU::fetch_nametable_address() { address_bus = 0x2000 | (v & 0xFFF); }

// The attribute address takes the form NN 1111 YYY XXX where N are the nametable select bits of v, 1111 is the attribute offset,
// and YYY and XXX are the upper 3 bits of the Y coarse and X coarse bits in v (essentially y/4 and x/4)
void PPU::fetch_attribute_address() { address_bus = 0x23C0 | (v & 0x0C00) | ((v >> 4) & 0x38) | ((v >> 2) | 0x07); }

// The pattern table address take the form 0 H NNNN NNNN P yyy where H determines which table is being fetched from (this comes from
// PPUCTRL) NNNN NNNN is the nametable tile number, P determines if it's the significant byte or the less significant byte, and yyy
// is fine y
void PPU::fetch_patterntable_low_address() { address_bus = (((uint16_t)ppuctrl << 7) & 0x1000) | (((uint16_t)current_nametable_byte << 4) & 0xFF0) | ((v >> 12) & 0x7); }

void PPU::fetch_patterntable_high_address() { address_bus = 8 | (((uint16_t)ppuctrl << 7) & 0x1000) | (((uint16_t)current_nametable_byte << 4) & 0xFF0) | ((v >> 12) & 0x7); }

// This oversees fetching for most cycles where the PPU needs to fetch data
void PPU::fetch() {
    if (!is_render_enabled()) return;

    int dot_mod = dot % 8;
    switch (dot_mod) {
        // Fetch the background high byte
        case 0:
            current_pattern_high_byte = read(address_bus);
            break;
        // Fetch nametable address
        case 1:
            fetch_nametable_address();
            break;
        // Fetch nametable byte
        case 2:
            current_nametable_byte = read(address_bus);
            break;
        // Fetch attribute address
        case 3:
            fetch_attribute_address();
            break;
        // Fetch attribute byte
        case 4:
            current_attribute_byte = read(address_bus);
            break;
        // Fetch pattern low address
        case 5:
            fetch_patterntable_low_address();
            break;
        // Fetch pattern low byte
        case 6:
            current_pattern_low_byte = read(address_bus);
            break;
        // Fetch pattern high address
        case 7:
            fetch_patterntable_high_address();
            break;
    }
}

void PPU::load_shift_registers() {

    // This low bit/latch is selected using the coarse x and y bits from v (speficially bit 1 and bit 5)
    uint8_t bit_selector = ((1 & (v >> 1)) * 2 + (1 & (v >> 5)) * 4);
    low_attribute_latch = (current_attribute_byte >> bit_selector) & 1;
    high_attribute_latch = (current_attribute_byte >> (bit_selector + 1)) & 1;
    low_pattern_sr = (low_pattern_sr & 0xFF00) | current_pattern_low_byte;
    high_pattern_sr = (high_pattern_sr & 0xFF00) | current_pattern_high_byte;

}

// Basically ripped straight from the wiki page on scrolling
// We need to wrap if coarse x overflows - in that case we set coarse x to 0 and switch the horizontal nametable
void PPU::increment_coarse_x() {

    if ((v & 0x1F) == 31) {
        v &= ~0x1F;
        v ^= 0x400; // This toggles the horizontal nametable bit
    }
    else v++;

}

// Also ripped from the wiki
// Increment fine y, overflow to coarse y, and lastly if that overflows, flip the vertical nametable bit like incrementing coarse x
void PPU::increment_fine_y() {

    // If fine y is less than 7
    if ((v & 0x7000) != 0x7000) v += 0x1000;
    else {
        // Set fine y to 0
        v &= ~0x7000;
        uint16_t coarse_y = (v & 0x3E0) >> 5;
        if (coarse_y == 29) {
            coarse_y = 0;
            // Flip vertical nametable bit
            v ^= 0x800;
        }
        // Coarse y can be set out of bounds, which does not cause the nametable to switch - this has some funky effects that some 
        // games rely on
        else if (coarse_y == 31) coarse_y = 0;
        else coarse_y++;

        v = (v & ~0x3E0) | (coarse_y << 5);
    }

}

// This selects bits from our shift registers and updates the corresponding buffer entry with the new pixel data
void PPU::update_pixel() {
    // If rendering is disabled, we set every pixel to be the background color
    // There is a slight nuance in that if v is in the 0x3F00 region, we output whatever color its pointing to
    if (!is_render_enabled()) {
        // TODO
        // Do stuff to set the whole frame to the background color
        return;
    }

    // x selects a bit from each shift register to create a 4 bit number
    uint8_t bit = 15 - x;
    uint8_t low_pixel_bit = (low_pattern_sr >> bit) & 1;
    uint8_t high_pixel_bit = (high_pattern_sr >> bit) & 1;
    uint8_t bg_pixel = (high_pixel_bit << 1) | low_pixel_bit;

    uint8_t low_palette_bit = (low_attribute_sr >> bit) & 1;
    uint8_t high_palette_bit = (high_attribute_sr >> bit) & 1;
    uint8_t palette = (high_palette_bit << 1) | low_palette_bit;
    // palette * 4 + pixel identifies a color in the background palette

    // TODO - will just focus on the background for now
    // Cross reference with sprite pixel data to determine which gets drawn

    // Once that is done, we are left with a 5 bit number S AA PP where S selects the background or sprite palette, A
    // is the attribute data (palette number selector), and P is the pattern table data (pixel value)
    uint8_t drawn_pixel = (palette << 2) | bg_pixel;
    uint16_t color_index = read(drawn_pixel | 0x3F00);

    // This byte is used to lookup a color in the system palette. On actual hardware, there is no RGB signal, but here we just store
    // a table of RGB values that someone else made
    // Check the ppumask to determine color emphasis - bit 5 emphasizes red, bit 6 emphsizes green, and bit 7 emphasizes blue, bit 0
    // sets the color to be greyscale
    if ((ppumask & 1) == 1) color_index &= 0x30;
    // We shift everything over left one so we get a 9 bit address
    uint16_t color_emphasis = ((uint16_t) ppumask & 0xE0) << 1;

    // Finally we update the frame buffer with the new color info
    int offset = (scanline * 256 + dot) * 4;
    // blue
    frame_buffer[offset] = sys_palette[color_index | color_emphasis][2];
    // green
    frame_buffer[offset + 1] = sys_palette[color_index | color_emphasis][1];
    // red
    frame_buffer[offset + 2] = sys_palette[color_index | color_emphasis][0];
    // alpha
    frame_buffer[offset + 3] = SDL_ALPHA_OPAQUE;

}

void PPU::shift_srs() {

    low_attribute_sr = (low_attribute_sr << 1) | low_attribute_latch;
    high_attribute_sr = (high_attribute_sr << 1) | high_attribute_latch;
    low_pattern_sr = (low_pattern_sr << 1) | 1;
    high_pattern_sr = (high_pattern_sr << 1) | 1;

}

// Used to check which portions of rendering are enabled
bool PPU::is_render_enabled(){ return is_background_enabled() || is_sprite_enabled(); }

bool PPU::is_background_enabled() { return ((ppumask >> 1) && 1) == 1; }

bool PPU::is_sprite_enabled() { return ((ppumask >> 2) && 1) == 1; }

// Write functions
void PPU::write(uint16_t address, uint8_t val) {
    address &= 0x3FFF;
    // Pattern table area - no mirroring
    if (address <= 0x1FFF) {
        memory[address] = val;
    }
    // Nametable/attribute tables. Mirroring depends on mapper in use
    else if (address <= 0x3EFF) {
        (this->*writes[memory_mapper])(address, val);
    }
    // Palette data
    else if (address <= 0x3FFF) {
        // This is a design departure from what I've been doing, but I can't be bothered to think how mirroring works in this region
        if (address >= 0x3F20) {
            address &= 0x3F1F;
        }

        // If we alter the background color
        if (address % 4 == 0) {
            for (int i = 0; i < 8; i++) {
                memory[(address + 4 * i) & 0x1F] = val;
            }
        }
        else {
            memory[address] = val;
        }
    }
}

// Default memory mapper
void PPU::default_write(uint16_t address, uint8_t &val) {
    memory[address] = val;
    // Nametable 1 (0x2000) and nametable 3 (0x2800) are the same table. Likewise nametable 2 and 4 are the same
    if (vertical_mirroring) {
        memory[(address + 0x800) % 0x1000];
    }
    // Nametable 1 = nametable 2
    else {
        memory[address ^ 0x400] = val;
    }
}

// Read function
uint8_t PPU::read(uint16_t address) {
    address &= 0x3FFF;
    // Pattern table area - no mirroring
    if (address <= 0x3EFF) {
        if (address >= 0x3000) {
            address &= ~0x1000;
        }
        return memory[address];
    }
    // Palette data
    else if (address <= 0x3FFF) {
        if (address >= 0x3F20) {
            address &= 0x3F1F;
        }

        return memory[address];
    }
}